@model proekt.Models.DoctorProfile
@inject proekt.Services.TranslationService Loc
@{
    ViewData["Title"] = "Edit Doctor Profile";
    // Convert total minutes back to HH:mm for the inputs
    string FormatTime(int mins) => $"{(mins / 60):D2}:{(mins % 60):D2}";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

<div class="profile-edit-page py-5">
    <div class="container" style="max-width: 800px;">
        <div class="content-card p-4 p-md-5">
            <div class="d-flex align-items-center gap-3 mb-4">
                <div class="icon-badge bg-primary-soft">
                    <i class="bi bi-person-badge text-primary fs-4"></i>
                </div>
                <div>
                    <h2 class="fw-bold mb-0">Doctor Profile Settings</h2>
                    <p class="text-muted mb-0">Manage your specialization, fees, and working hours</p>
                </div>
            </div>

            @if (TempData["ProfileMessage"] != null)
            {
                <div class="alert alert-success rounded-4 mb-4 border-0 shadow-sm">
                    <i class="bi bi-check-circle-fill me-2"></i>@TempData["ProfileMessage"]
                </div>
            }

            <form asp-action="DoctorProfileEdit" method="post">
                @Html.AntiForgeryToken()

                <div class="row g-4">
                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-uppercase text-muted">Specialization</label>
                        <input type="text" name="specialization" value="@Model.Specialization" class="form-control rounded-3" placeholder="e.g. Cardiologist" required>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-uppercase text-muted">Consultation Fee (BGN)</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0 rounded-start-3"><i class="bi bi-cash-coin"></i></span>
                            <input type="number" step="0.01" name="consultationFee" value="@Model.ConsultationFee" class="form-control border-start-0 rounded-end-3" required>
                        </div>
                    </div>

                    <div class="col-12">
                        <label class="form-label fw-bold small text-uppercase text-muted">Professional Bio</label>
                        <textarea name="bio" class="form-control rounded-3" rows="4" placeholder="Briefly describe your experience and expertise...">@Model.Bio</textarea>
                    </div>

                    <div class="col-md-4">
                        <label class="form-label fw-bold small text-uppercase text-muted">Available From</label>
                        <input type="time" name="availableFrom" value="@FormatTime(Model.AvailableFromMinutes)" class="form-control rounded-3" required>
                    </div>

                    <div class="col-md-4">
                        <label class="form-label fw-bold small text-uppercase text-muted">Available To</label>
                        <input type="time" name="availableTo" value="@FormatTime(Model.AvailableToMinutes)" class="form-control rounded-3" required>
                    </div>

                    <div class="col-md-4">
                        <label class="form-label fw-bold small text-uppercase text-muted">Slot Duration (Min)</label>
                        <select name="slotDuration" class="form-select rounded-3">
                            <option value="15" selected="@(Model.SlotDurationMinutes == 15)">15 Minutes</option>
                            <option value="20" selected="@(Model.SlotDurationMinutes == 20)">20 Minutes</option>
                            <option value="30" selected="@(Model.SlotDurationMinutes == 30)">30 Minutes</option>
                            <option value="45" selected="@(Model.SlotDurationMinutes == 45)">45 Minutes</option>
                            <option value="60" selected="@(Model.SlotDurationMinutes == 60)">60 Minutes</option>
                        </select>
                    </div>

                    <div class="col-12">
                        <label class="form-label fw-bold small text-uppercase text-muted">Working Days</label>
                        <div class="d-flex flex-wrap gap-2">
                            @foreach (var day in new[] { (1, "Mon"), (2, "Tue"), (3, "Wed"), (4, "Thu"), (5, "Fri"), (6, "Sat"), (7, "Sun") })
                            {
                                var isChecked = Model.WorkingDays.Split(',').Contains(day.Item1.ToString());
                                <div class="day-check">
                                    <input type="checkbox" name="workingDaysArray" value="@day.Item1" id="day-@day.Item1" @(isChecked ? "checked" : "") class="btn-check">
                                    <label class="btn btn-outline-primary rounded-pill px-3" for="day-@day.Item1">@day.Item2</label>
                                </div>
                            }
                        </div>
                        <input type="hidden" name="workingDays" id="workingDaysHidden" value="@Model.WorkingDays">
                    </div>

                    <div class="col-12 mt-5">
                        <div class="d-flex gap-3">
                            <button type="submit" class="btn btn-primary px-5 py-2 rounded-pill fw-bold shadow-sm">
                                <i class="bi bi-save2 me-2"></i>Save Profile
                            </button>
                            <a asp-action="Profile" class="btn btn-light px-4 py-2 rounded-pill fw-bold text-muted border">Cancel</a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    .profile-edit-page { background: #f8faff; min-height: 100vh; }
    .content-card { background: white; border-radius: 24px; box-shadow: 0 10px 40px rgba(0,0,0,0.05); border: 1px solid #edf2f7; }
    .icon-badge { width: 48px; height: 48px; border-radius: 14px; display: flex; align-items: center; justify-content: center; }
    .bg-primary-soft { background: rgba(0,82,204,0.1); }
    .form-control:focus, .form-select:focus { border-color: #0052CC; box-shadow: 0 0 0 4px rgba(0,82,204,0.1); }
    .btn-outline-primary { border-width: 2px; }
    .btn-check:checked + .btn-outline-primary { background-color: #0052CC; border-color: #0052CC; color: white; }
</style>

@section Scripts {
    <script>
        document.querySelectorAll('input[name="workingDaysArray"]').forEach(cb => {
            cb.addEventListener('change', () => {
                const checked = Array.from(document.querySelectorAll('input[name="workingDaysArray"]:checked'))
                                    .map(el => el.value)
                                    .join(',');
                document.getElementById('workingDaysHidden').value = checked;
            });
        });
    </script>
}
